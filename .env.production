# Public config baked into the deployed build (safe to commit — not secrets).
# These point the gallery at the repository whose branches are the templates.
VITE_GITHUB_OWNER=raman365
VITE_GITHUB_REPO=sanity-templates
