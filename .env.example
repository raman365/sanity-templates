# Sanity configuration — copy this file to .env.local and fill in your values.
# Find both values at https://www.sanity.io/manage (or create a new project there).

NEXT_PUBLIC_SANITY_PROJECT_ID="your-project-id"
NEXT_PUBLIC_SANITY_DATASET="production"
